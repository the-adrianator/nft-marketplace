import Head from 'next/head';
import Image from 'next/image';

const Home = () => (
  <div>
    <h1 className="text-3xl font-bold underline">
      Hello world!
    </h1>
  </div>
);

export default Home;
